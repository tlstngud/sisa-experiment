# SSM-Informed Softmax Attention (SISA)

## 실험 설계서 v2.3.1 Final — H100 80GB

> **핵심 아이디어**: SSM의 선택적 상태 추적 신호를 Transformer의 attention score에
> **score 수준에서 직접 fusion**하여, 하나의 내적 연산으로 두 능력을 통합한다.

> **v2.3 → v2.3.1 변경 (출발 전 마지막 정리):**
> - 환경 pinning: exact torch/CUDA 조합 고정 (version floor → exact pin)
> - SDPA flash backend smoke test 추가 (Phase 0 체크리스트)
> - Reduced-MLP Transformer를 보조 control로 분리, 파라미터/시간 정정
> - Mamba-2 block count 러프 추정 반영 (31L/49L)
> - RMSNorm dtype bug fix
> - Phase 0 모니터링 항목 추가: max|g-c|, λ 분포, effective half-life
> - 데이터 문구 정확화: "EOS-concatenated blocks without document masks"

---

## 1. 포지셔닝: Score-Level Fusion

기존 Transformer-SSM 하이브리드는 모두 block/head/output 수준에서 결합한다:

| 모델 | 결합 수준 | 방식 |
|------|----------|------|
| Jamba | Block-level | Transformer/Mamba 레이어 interleave |
| Hymba | Head-level | 같은 레이어에서 attn head + SSM head 병렬 |
| Falcon-H1 | Head-level | attention + SSM 같은 블록에서 병렬 |
| Samba | Block-level | Mamba + Sliding Window Attention 교차 |
| **SISA** | **Score-level** | **SSM 신호를 attention score 내적에 직접 융합** |

SISA의 차별점은 "하이브리드"가 아니라,
**하나의 내적(Q̂^T K̂)이 유사도와 SSM 중요도를 동시에 계산**한다는 것이다.

---

## 2. 수식

### 2.1 SISA Score (Additive Formulation)

```
score_{ij} = q_i^T k_j / √d_h  +  λ · C̄_i^T · B̄_j       (i ≥ j)
```

- 첫째 항: Transformer의 "이 토큰이 쿼리와 유사한가?" (content similarity)
- 둘째 항: SSM의 "이 토큰이 시퀀스 맥락에서 중요한가?" (structured dynamic bias)

### 2.2 Augmented Q/K Exact Rewrite (핵심 정리)

**Proposition (SISA Score Decomposition).**
위의 additive score는 augmented query/key의 scaled inner product로 exact하게 재표현된다:

```
Q̂_i = [ q_i,   d_h^{1/4} · √λ · C̄_i ]       dim = d_h + d_state
K̂_j = [ k_j,   d_h^{1/4} · √λ · B̄_j ]       dim = d_h + d_state

score_{ij} = Q̂_i^T · K̂_j  /  √d_h
```

**증명:**
```
Q̂_i^T · K̂_j / √d_h

= ( q_i^T k_j  +  d_h^{1/4}·√λ·C̄_i^T · d_h^{1/4}·√λ·B̄_j ) / √d_h

= ( q_i^T k_j  +  d_h^{1/2} · λ · C̄_i^T B̄_j ) / √d_h

= q_i^T k_j / √d_h  +  λ · C̄_i^T B̄_j    ∎
```

**의의:**
1. L×L 행렬 M_ssm을 만들 필요 없음
2. 표준 causal attention 커널을 그대로 사용 가능
3. 단, softmax_scale = 1/√d_h로 명시 필수 (기본값 1/√augmented_dim과 다름)

### 2.3 C̄, B̄ 정의 (Mamba-3에서 영감)

Mamba-3의 complex SSM ↔ data-dependent RoPE 등가성을 차용:

```
α_t = exp(-softplus(W_α · x_t))             ∈ (0, 1), 안정적 감쇠율
θ_t = W_θ · x_t                              data-dependent 회전 주파수

g_t = Σ_{k≤t} log α_k                       누적 감쇠 (FP32)
Φ_t = Σ_{k≤t} θ_k                           누적 위상 (FP32)
c = (max(g) + min(g)) / 2                    sequence-global minimax offset

C̄_i = e^{g_i - c}  · R(Φ_i) · C_i          감쇠 스케일링 + 회전
B̄_j = e^{-g_j + c} · R(Φ_j) · B_j          감쇠 스케일링 + 회전
```

R(Φ)는 2×2 회전 행렬의 block-diagonal 적용 (data-dependent RoPE).

**c 상수 규칙**: c는 반드시 **sequence-global 상수**.
minimax (max+min)/2가 mean보다 좋은 이유:
최대 exponent 크기를 양방향으로 균등하게 억제하여 overflow 방지.

**RoPE 분리 규칙**: positional RoPE는 q, k 채널에만 적용.
C̄, B̄ 채널에는 SSM 자체의 data-dependent RoPE만 적용. 혼용 금지.

### 2.4 수치 안정성

**α 초기화:**
```python
# target half-life ~100 tokens
# α = exp(-softplus(raw)), softplus(-5) ≈ 0.0067 → α ≈ 0.9933
# half-life = log(0.5) / log(0.9933) ≈ 103
self.alpha_proj = nn.Linear(d_model, n_head, bias=True)
nn.init.constant_(self.alpha_proj.bias, -5.0)
```

**λ 양수 제약:**
```python
self.lambda_raw = nn.Parameter(torch.full((n_head,), -1.0))
# softplus(-1) ≈ 0.31

@property
def lambda_(self):
    return F.softplus(self.lambda_raw)   # 항상 > 0
```

---

## 3. 데이터 경로: Packing 없음 (메인)

### 3.1 설계 결정

**메인 실험에서는 document packing을 사용하지 않는다.**

이유:
- Packing은 segmented prefix sum (현재 Python for-loop) 필요 → 성능 병목
- FlexAttention block_mask 생성 비용
- SSM prefix sum과 attention mask의 document boundary 정합성 검증 부담
- 핵심 novelty는 "score-level fusion"이지 "efficient packing"이 아님

**대신:**
- 각 시퀀스는 **EOS-concatenated 2048-token block without document masks**
- 문서가 2048보다 길면 연속 추출, 짧으면 다음 문서를 EOS 토큰으로 이어붙여서 2048 채움
- 엄밀한 "one document per sequence"가 아니라 EOS-delimited continuous block
- is_causal=True만으로 충분, 별도 document mask 불필요
- g, Φ는 torch.cumsum으로 계산 (segmented 불필요)
- **주의**: synthetic state-tracking task (Parity 등)는 한 샘플 = 한 블록으로 분리하여 분석 깔끔하게 유지

이 설정은 Mamba 원 논문, Mamba-2 논문과 동일한 데이터 처리 방식이며,
3개 모델 간 **완전히 동일한 데이터를 동일한 방식**으로 보게 된다.

**Packing + parallel scan + FlexAttention은 후속 연구(v3)로 이동.**

### 3.2 Mamba-2 데이터 주의사항

Mamba-2는 positional embedding이 없고, right-padding이 noise를 전파할 수 있다.
EOS-terminated block 방식은 padding이 없으므로 이 문제를 자연스럽게 회피한다.

---

## 4. 모델 설계 — 정확한 파라미터 예산

### 4.1 공통 설정

| 설정 | 값 |
|------|-----|
| Vocab | 50,277 (GPT-NeoX) |
| Embedding | Tied (input = output) |
| Norm | RMSNorm (pre-norm) |
| MLP | SwiGLU (W_gate, W_up, W_down = 3개 행렬) |
| Position | RoPE (Transformer, SISA) / 없음 (Mamba-2) |
| Max Seq Len | 2,048 |

### 4.2 Phase 1: 빠른 검증 (~152M)

#### (A) Transformer Baseline

```
Config: 12 layers, d=768, n_head=12, d_head=64, d_ff=3072

Embedding (tied):  50,277 × 768                    = 38,612,736
Per layer:
  W_Q + W_K + W_V + W_O = 4 × 768²                =  2,359,296
  SwiGLU = 3 × 768 × 3072                           =  7,077,888
  RMSNorm × 2 = 2 × 768                            =      1,536
  Layer subtotal                                     =  9,438,720
12 layers                                           =113,264,640
Final RMSNorm                                       =        768
─────────────────────────────────────────────────────
Total                                               =151,878,144 ≈ 152M
```

#### (A') Reduced-MLP Transformer (보조 Control)

SISA와 동일한 d_ff로 MLP를 줄인 Transformer.
**보조 control**: "MLP 축소 자체의 영향"을 격리하는 목적. 메인 baseline이 아님.

```
Config: 12 layers, d=768, n_head=12, d_head=64, d_ff=2748

Per layer:
  QKVO = 2,359,296
  SwiGLU = 3 × 768 × 2748                           =  6,331,392
  RMSNorm × 2                                       =      1,536
  Layer subtotal                                     =  8,692,224
12 layers                                           =104,306,688
Embedding + Final Norm                               = 38,613,504
─────────────────────────────────────────────────────
Total                                               ≈142,920,192 ≈ 143M
```

파라미터 수가 A/B/C(152M)보다 ~9M 적음.
목적이 "MLP 축소 효과 분리"이므로 이 차이는 의도적이며 허용.

#### (B) Mamba-2 Baseline

공식 state-spaces/mamba repo 기준:
- Mamba-2 블록은 대략 `3 × expand × d_model²` 파라미터
- comparable Transformer 대비 layer count 약 2배

```
Config: ~31 layers, d=768, d_state=64, expand=2, d_conv=4

러프 추정:
  Mamba-2 block ≈ 3 × 2 × 768² ≈ 3,538,944/block
  목표 152M에서 embedding(38.6M)을 빼면 body ≈ 113M
  113M / 3.54M ≈ 31~32 blocks

→ 31 blocks로 시작, sum(p.numel())로 152M ± 1%에 맞춤.
   안 맞으면 expand 또는 layer 수 미세조정.
   반드시 Phase 1 학습 전에 확정.
```

#### (C) SISA

```
Config: 12 layers, d=768, n_head=12, d_head=64, d_ff=2748
        SSM: d_state=32, R=1, augmented_dim = 64+32 = 96

Embedding (tied):  50,277 × 768                    = 38,612,736
Per layer:
  --- Attention (Transformer와 동일) ---
  W_Q + W_K + W_V + W_O = 4 × 768²                =  2,359,296
  
  --- SSM channels ---
  W_B:     768 × (12 × 32) = 768 × 384             =    294,912
  W_C:     768 × 384                                =    294,912
  W_α:     768 × 12 + 12 (bias)                    =      9,228
  W_θ:     768 × (12 × 16) = 768 × 192             =    147,456
  λ_raw:   12                                       =         12
  SSM subtotal                                       =    746,520
  
  --- SwiGLU MLP (d_ff 축소) ---
  3 × 768 × 2748                                    =  6,331,392
  
  --- RMSNorm × 2 ---
  2 × 768                                           =      1,536
  
  Layer subtotal = 2,359,296 + 746,520 + 6,331,392 + 1,536 = 9,438,744
  
12 layers                                           =113,264,928
Final RMSNorm                                       =        768
Embedding (tied)                                    = 38,612,736
─────────────────────────────────────────────────────
Total                                               ≈151,878,432 ≈ 152M ✓
```

**Transformer 152M ≈ SISA 152M  ✓  (차이 < 300 params)**

### 4.3 Phase 2: 본 실험 (~369M)

#### (A) Transformer Baseline

```
Config: 24 layers, d=1024, n_head=16, d_head=64, d_ff=2944

Embedding (tied):  50,277 × 1024                   = 51,483,648
Per layer:
  QKVO: 4 × 1024²                                  =  4,194,304
  SwiGLU: 3 × 1024 × 2944                           =  9,043,968
  RMSNorm × 2                                       =      2,048
  Layer subtotal                                     = 13,240,320
24 layers                                           =317,767,680
Final RMSNorm                                       =      1,024
─────────────────────────────────────────────────────
Total                                               ≈369,252,352 ≈ 369M
```

#### (A') Reduced-MLP Transformer (보조 Control)

```
Config: 24 layers, d=1024, d_ff=2512
SwiGLU: 3 × 1024 × 2512                             =  7,716,864
Layer subtotal = 4,194,304 + 7,716,864 + 2,048      = 11,913,216
24 layers                                            =285,917,184
Total (with embed)                                   ≈337,401,856 ≈ 337M
```

보조 control. 메인 비교 대상이 아님.

#### (B) Mamba-2 Baseline

```
Config: ~49 layers, d=1024, d_state=64, expand=2

러프 추정:
  Mamba-2 block ≈ 3 × 2 × 1024² ≈ 6,291,456/block
  목표 369M에서 embedding(51.5M)을 빼면 body ≈ 318M
  318M / 6.29M ≈ 50~51 blocks → 49 blocks로 시작

→ sum(p.numel())로 369M ± 1%에 맞춤.
   반드시 Phase 2 학습 전에 확정.
```

#### (C) SISA

```
Config: 24 layers, d=1024, n_head=16, d_head=64, d_ff=2512
        SSM: d_state=32, R=1, augmented_dim=96

Per layer SSM:
  W_B: 1024 × (16 × 32) = 1024 × 512               =    524,288
  W_C: 1024 × 512                                    =    524,288
  W_α: 1024 × 16 + 16                               =     16,400
  W_θ: 1024 × (16 × 16) = 1024 × 256                =    262,144
  λ_raw: 16                                          =         16
  SSM subtotal                                        =  1,327,136

  SwiGLU: 3 × 1024 × 2512                            =  7,716,864
  QKVO: 4 × 1024²                                    =  4,194,304
  RMSNorm × 2                                        =      2,048
  Layer subtotal = 4,194,304 + 1,327,136 + 7,716,864 + 2,048 = 13,240,352

24 layers                                            =317,768,448
Embedding + Final Norm                                = 51,484,672
─────────────────────────────────────────────────────
Total                                                ≈369,253,120 ≈ 369M ✓
```

**Transformer 369M ≈ SISA 369M  ✓  (차이 < 800 params)**

### 4.4 요약표

**메인 비교 (파라미터 매칭):**

| 모델 | Phase 1 | Phase 2 | 역할 |
|------|---------|---------|------|
| **(A) Transformer** | 12L, d=768, d_ff=3072 → **152M** | 24L, d=1024, d_ff=2944 → **369M** | 메인 baseline |
| **(B) Mamba-2** | ~31L, d=768 → **~152M** | ~49L, d=1024 → **~369M** | SSM baseline |
| **(C) SISA** | 12L, d_ff=2748, d_state=32 → **152M** | 24L, d_ff=2512, d_state=32 → **369M** | 우리 모델 |

**보조 Control (파라미터 매칭 아님):**

| 모델 | Phase 1 | Phase 2 | 역할 |
|------|---------|---------|------|
| **(A') Reduced-MLP Transformer** | 12L, d_ff=2748 → **143M** | 24L, d_ff=2512 → **337M** | MLP 축소 효과 분리 |

A'는 SISA와 동일한 d_ff를 사용하되 SSM branch가 없는 control.
"SISA의 이점이 SSM에서 오는가, 아니면 단순히 MLP 축소/구조 변경 효과인가?"를 분리.

모든 모델의 최종 파라미터 수는 **구현 후 `sum(p.numel())`로 확정**하고, 1% 이상 차이 시 layer/d_ff 미세조정.
Mamba-2는 **반드시 Phase 1 학습 전에 exact count 확정**.

---

## 5. SISA Block 구조 (최종)

```
Input x
  │
  ├── RMSNorm
  │
  │ ─── Attention Path ───
  ├──→ q = W_Q(x),  k = W_K(x),  v = W_V(x)
  ├──→ q, k = apply_positional_RoPE(q, k)       ← q, k만! C̄, B̄에는 안 함
  │
  │ ─── SSM Path ───
  ├──→ B_ssm = W_B(x),  C_ssm = W_C(x)          ← 원본 x에서 프로젝션
  ├──→ α = exp(-softplus(W_α(x)))                ← 안정적 감쇠
  ├──→ θ = W_θ(x)                                 ← data-dependent 회전 주파수
  ├──→ g = cumsum(log α, dim=L)                   ← 누적 감쇠, FP32
  ├──→ Φ = cumsum(θ, dim=L)                       ← 누적 위상, FP32
  ├──→ c = (max(g) + min(g)) / 2                  ← sequence-global offset
  ├──→ C̄ = e^{g-c} · R(Φ) · C_ssm               ← 감쇠 + 회전
  ├──→ B̄ = e^{-g+c} · R(Φ) · B_ssm
  │
  │ ─── Augmented Q/K (대칭 scaling) ───
  ├──→ λ = softplus(λ_raw)                        ← per-head, 양수
  ├──→ s = d_h^{1/4} · √λ                         ← 대칭 scaling factor
  ├──→ Q̂ = concat([ q,  s · C̄ ], dim=-1)         ← (B, H, L, d_h + d_state)
  ├──→ K̂ = concat([ k,  s · B̄ ], dim=-1)
  │
  │ ─── Single Attention ───
  ├──→ Y = SDPA(Q̂, K̂, V, scale=1/√d_h, is_causal=True)
  │         ↑ 하나의 연산: 유사도 + SSM 중요도 동시 반영
  │
  └──→ Output = W_O(Y) + residual

  │
  ├── RMSNorm
  └── SwiGLU MLP + residual
```

---

## 6. 구현

### 6.1 경로

| 경로 | 라이브러리 | 용도 |
|------|----------|------|
| **메인** | **PyTorch SDPA** (is_causal=True) | Phase 0~2 전체 |
| Ablation (gated 등) | FlexAttention (score_mod) | 비-additive 변형 |
| 후속 (v3, packing) | FlexAttention (block_mask) | document packing 지원 |

메인을 SDPA로 두는 이유:
- Packing 없으니 is_causal=True만으로 충분
- 안정적이고 검증된 경로
- FlexAttention은 아직 prototype (torch>=2.5.0)

### 6.2 환경 (Exact Pinning)

**메인 실험 환경 (실제 설치 확인 완료):**
```bash
# Exact versions — version floor가 아니라 고정 pin
torch==2.4.1+cu121              # SDPA + torch.compile (CUDA toolkit 12.2 대응)
cuda-toolkit==12.2              # H100 PCIe MIG 1g.10gb
mamba-ssm==2.2.2                # Mamba-2 baseline
causal-conv1d>=1.4.0            # mamba-ssm 의존성
lm-eval==0.4.2                  # 벤치마크
wandb                           # 로깅
datasets                        # SlimPajama
transformers                    # GPT-NeoX tokenizer
python==3.11                    # venv 기준
```

**하드웨어**: H100 PCIe, MIG 1g.10gb (SM 14개, VRAM ~10GB)
micro batch를 줄이고 gradient accumulation을 늘려 effective batch 유지.

`flash-attn` 패키지는 **선택 의존성**이다.
PyTorch SDPA는 내부에 FlashAttention-2 / memory-efficient / math 구현을 포함하고 있으며,
입력 shape에 따라 fused kernel을 자동 선택한다.
별도 `flash-attn` 패키지 없이도 SDPA flash backend가 작동한다.

**실제 확인 결과 (Phase 0 smoke test):**
augmented Q/K (dim=96) + V (dim=64)는 Q/K와 V의 last dim이 달라
Flash Attention backend 불가. **EFFICIENT_ATTENTION** backend로 정상 fallback 확인.
학습에는 문제 없음.

**Ablation 전용 환경 (FlexAttention):**
```bash
torch==2.5.1+cu124              # FlexAttention prototype 지원
# 메인 실험과 별도 환경. FlexAttention은 아직 prototype feature.
```

### 6.3 SDPA Flash Backend Smoke Test (Phase 0 필수)

실험 시작 전에 augmented Q/K shape에서 flash backend가 실제로 잡히는지 확인:

```python
from torch.nn.attention import sdpa_kernel, SDPBackend

# 실제 실험 shape
B, H, L = 2, 12, 256     # Phase 0 shape
d_aug = 96                 # d_head(64) + d_state(32)
d_v = 64

Q_test = torch.randn(B, H, L, d_aug, device='cuda', dtype=torch.bfloat16)
K_test = torch.randn(B, H, L, d_aug, device='cuda', dtype=torch.bfloat16)
V_test = torch.randn(B, H, L, d_v,   device='cuda', dtype=torch.bfloat16)

# Flash backend 강제 → 안 되면 경고 출력
with sdpa_kernel(backends=[SDPBackend.FLASH_ATTENTION]):
    try:
        Y = F.scaled_dot_product_attention(Q_test, K_test, V_test,
                                            scale=1.0/8.0, is_causal=True)
        print("✅ Flash backend OK")
    except RuntimeError as e:
        print(f"⚠️ Flash backend 불가: {e}")
        print("→ memory-efficient 또는 math backend로 fallback")
```

augmented_dim=96은 FlashAttention 지원 범위(최대 256) 내이므로 잡힐 가능성이 높지만,
**가정하지 말고 확인**한다.

### 6.3 핵심 코드 (최종)

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
import math


class RMSNorm(nn.Module):
    def __init__(self, dim, eps=1e-6):
        super().__init__()
        self.weight = nn.Parameter(torch.ones(dim))
        self.eps = eps
    
    def forward(self, x):
        norm = torch.rsqrt(x.float().pow(2).mean(-1, keepdim=True) + self.eps)
        return ((x.float() * norm) * self.weight.float()).to(x.dtype)


class SISALayer(nn.Module):
    """
    SSM-Informed Softmax Attention.
    
    Core equation:
        score_ij = q_i^T k_j / √d_h  +  λ · C̄_i^T · B̄_j
    
    Implemented as:
        Q̂ = [q, s·C̄],  K̂ = [k, s·B̄],  s = d_h^{1/4}·√λ
        Y = SDPA(Q̂, K̂, V, scale=1/√d_h, is_causal=True)
    """
    
    def __init__(self, d_model, n_head, d_state=32, d_ff=2748):
        super().__init__()
        assert d_model % n_head == 0
        assert d_state % 2 == 0  # RoPE needs even d_state
        
        self.n_head = n_head
        self.d_head = d_model // n_head
        self.d_state = d_state
        self.d_model = d_model
        
        # ─── Attention ───
        self.W_Q = nn.Linear(d_model, d_model, bias=False)
        self.W_K = nn.Linear(d_model, d_model, bias=False)
        self.W_V = nn.Linear(d_model, d_model, bias=False)
        self.W_O = nn.Linear(d_model, d_model, bias=False)
        
        # ─── SSM projections ───
        self.W_B = nn.Linear(d_model, n_head * d_state, bias=False)
        self.W_C = nn.Linear(d_model, n_head * d_state, bias=False)
        
        # ─── SSM dynamics ───
        self.alpha_proj = nn.Linear(d_model, n_head, bias=True)
        nn.init.constant_(self.alpha_proj.bias, -5.0)   # half-life ~100 tokens
        
        self.theta_proj = nn.Linear(d_model, n_head * (d_state // 2), bias=False)
        nn.init.normal_(self.theta_proj.weight, std=0.01)
        
        # ─── λ: per-head, positive ───
        self.lambda_raw = nn.Parameter(torch.full((n_head,), -1.0))
        
        # ─── Norms ───
        self.attn_norm = RMSNorm(d_model)
        self.ffn_norm = RMSNorm(d_model)
        
        # ─── SwiGLU MLP ───
        self.w_gate = nn.Linear(d_model, d_ff, bias=False)
        self.w_up = nn.Linear(d_model, d_ff, bias=False)
        self.w_down = nn.Linear(d_ff, d_model, bias=False)
    
    def forward(self, x):
        """
        x: (B, L, D)
        Returns: (B, L, D)
        """
        B, L, D = x.shape
        H = self.n_head
        d_h = self.d_head
        d_s = self.d_state
        
        # ═══════ Attention Sublayer ═══════
        residual = x
        x_norm = self.attn_norm(x)
        
        # --- Attention projections ---
        q = self.W_Q(x_norm).view(B, L, H, d_h).transpose(1, 2)   # (B, H, L, d_h)
        k = self.W_K(x_norm).view(B, L, H, d_h).transpose(1, 2)
        v = self.W_V(x_norm).view(B, L, H, d_h).transpose(1, 2)
        
        # Positional RoPE (q, k 채널만!)
        q, k = apply_rotary_emb(q, k)
        
        # --- SSM projections ---
        B_ssm = self.W_B(x_norm).view(B, L, H, d_s).transpose(1, 2)  # (B, H, L, d_state)
        C_ssm = self.W_C(x_norm).view(B, L, H, d_s).transpose(1, 2)
        
        # --- Decay g (FP32, cumsum along L axis) ---
        log_alpha = -F.softplus(self.alpha_proj(x_norm))           # (B, L, H)
        log_alpha = log_alpha.transpose(1, 2).float()              # (B, H, L)
        g = torch.cumsum(log_alpha, dim=2)                         # (B, H, L) ← dim=2 = L축
        
        # --- Phase Φ (FP32, cumsum along L axis) ---
        theta = self.theta_proj(x_norm)                             # (B, L, H * d_state//2)
        theta = theta.view(B, L, H, d_s // 2).permute(0, 2, 1, 3).float()  # (B, H, L, d_s//2)
        Phi = torch.cumsum(theta, dim=2)                            # (B, H, L, d_s//2) ← dim=2 = L축
        
        # --- Sequence-global c offset (minimax) ---
        c = (g.amax(dim=2, keepdim=True) + g.amin(dim=2, keepdim=True)) / 2  # (B, H, 1)
        
        # --- Apply decay scaling ---
        C_bar = C_ssm.float() * torch.exp((g - c).unsqueeze(-1))   # (B, H, L, d_state)
        B_bar = B_ssm.float() * torch.exp((-g + c).unsqueeze(-1))
        
        # --- Apply data-dependent RoPE on SSM channels ---
        C_bar = self._apply_ssm_rope(C_bar, Phi).to(x.dtype)
        B_bar = self._apply_ssm_rope(B_bar, Phi).to(x.dtype)
        
        # ═══════ Augmented Q/K (대칭 scaling) ═══════
        lam = F.softplus(self.lambda_raw)                           # (H,)
        s = (d_h ** 0.25) * torch.sqrt(lam)                        # (H,)
        s = s.view(1, H, 1, 1)                                     # (1, H, 1, 1) broadcast
        
        Q_aug = torch.cat([q, s * C_bar], dim=-1)                  # (B, H, L, d_h + d_state)
        K_aug = torch.cat([k, s * B_bar], dim=-1)                  # (B, H, L, d_h + d_state)
        
        # ═══════ Single Attention ═══════
        Y = F.scaled_dot_product_attention(
            Q_aug, K_aug, v,
            scale=1.0 / math.sqrt(d_h),    # 반드시 d_h 기준!
            is_causal=True
        )                                                           # (B, H, L, d_h)
        
        Y = Y.transpose(1, 2).reshape(B, L, D)
        x = self.W_O(Y) + residual
        
        # ═══════ FFN Sublayer ═══════
        residual = x
        h = self.ffn_norm(x)
        x = self.w_down(F.silu(self.w_gate(h)) * self.w_up(h)) + residual
        
        return x
    
    def _apply_ssm_rope(self, z, Phi):
        """
        Data-dependent RoPE on SSM channels.
        z:   (B, H, L, d_state)
        Phi: (B, H, L, d_state//2)
        """
        B, H, L, D = z.shape
        half_D = D // 2
        
        z_even = z[..., 0::2]    # (B, H, L, d_state//2)
        z_odd  = z[..., 1::2]    # (B, H, L, d_state//2)
        
        cos_phi = torch.cos(Phi)  # (B, H, L, d_state//2)
        sin_phi = torch.sin(Phi)
        
        out_even = z_even * cos_phi - z_odd * sin_phi
        out_odd  = z_even * sin_phi + z_odd * cos_phi
        
        # Interleave back
        out = torch.stack([out_even, out_odd], dim=-1)   # (B, H, L, d_state//2, 2)
        return out.view(B, H, L, D)
```

---

## 7. 학습 설정

### 7.1 데이터

| 항목 | Phase 1 | Phase 2 |
|------|---------|---------|
| 데이터셋 | DKYoon/SlimPajama-6B (streaming) | MBZUAI-LLM/SlimPajama-627B-DC (streaming) |
| 토큰 수 | 5B (~7GB) | 20B (~29GB) |
| Tokenizer | GPT-NeoX (50,277) | 동일 |
| Seq Len | 2,048 | 2,048 |
| **Packing** | **없음** (EOS-terminated 2048 block) | **없음** |
| 문서 처리 | 연속 추출, 짧은 문서는 EOS로 이어붙이기 | 동일 |

### 7.2 하이퍼파라미터

| 항목 | Phase 1 (152M) | Phase 2 (369M) |
|------|----------------|----------------|
| Optimizer | AdamW | AdamW |
| β1, β2 | 0.9, 0.95 | 0.9, 0.95 |
| LR (main) | 6e-4 | 3e-4 |
| LR (λ_raw) | 6e-5 (10× 작게) | 3e-5 |
| Schedule | Cosine + Warmup | Cosine + Warmup |
| Warmup | 500 steps | 2,000 steps |
| Weight Decay | 0.1 (λ_raw는 0.0) | 0.1 (λ_raw는 0.0) |
| Micro Batch | 4 (10GB MIG 대응) | 2 (10GB MIG 대응) |
| Grad Accum | 64 | 128 |
| Effective Batch | ~524K tokens | ~524K tokens |
| Grad Clip | 1.0 | 1.0 |
| Precision | BF16 (g, Φ cumsum은 FP32) | 동일 |
| torch.compile | 사용 | 사용 |

### 7.3 H100 학습 시간

**실제 환경: H100 PCIe MIG 1g.10gb (SM 14개, VRAM ~10GB)**

H100 PCIe full 기준 BF16 dense ~756 TFLOPS.
MIG 1g.10gb는 SM 14/114개 (약 1/8 파티션).
추정 처리량: 756 × (14/114) × MFU 30% ≈ **28 TFLOPS** (MIG + 작은 micro batch 오버헤드 반영)

| Phase | 모델 | 파라미터 | 계산 | 시간 (MIG 추정) |
|-------|------|---------|------|------|
| 0 | Toy SISA | 10M | 6×10M×50M / 28T | ~1.8분 |
| 1 | (A) Transformer | 152M | 6×152M×5B / 28T | ~45시간 |
| 1 | (B) Mamba-2 | 152M | 동일 | ~45시간 |
| 1 | (C) SISA | 152M | 동일 | ~45시간 |
| 1 | (A') Reduced-MLP | 143M | 6×143M×5B / 28T | ~42시간 |
| **1 합계** | | | | **~177시간 ≈ 7.4일** |
| 2 | (A) Transformer | 369M | 6×369M×20B / 28T | ~440시간 |
| 2 | (B) Mamba-2 | 369M | 동일 | ~440시간 |
| 2 | (C) SISA | 369M | 동일 | ~440시간 |
| 2 | (A') Reduced-MLP | 337M | 6×337M×20B / 28T | ~400시간 |
| **2 합계** | | | | **~1,720시간 ≈ 72일** |

> **참고**: 위 시간은 MIG 1g.10gb 환경의 러프 추정.
> MIG를 해제하고 full H100 80GB를 사용할 경우, 원래 설계서 추정치
> (Phase 1 ~12.6시간, Phase 2 ~5일)에 가까워진다.

---

## 8. 평가

### 8.1 기본 언어 능력 ("멍청해지지 않았는가?")

| 벤치마크 | Shot | 통과 기준 |
|---------|------|----------|
| Validation PPL | - | Transformer ± 2% |
| LAMBADA | 0 | ± 1% |
| HellaSwag | 0 | ± 1% |
| PIQA | 0 | ± 1% |
| ARC-Easy | 0 | ± 1% |
| WinoGrande | 0 | ± 1% |

### 8.2 State Tracking ("SSM 복소 역학이 기여하는가?")

| 벤치마크 | 기대 |
|---------|------|
| Parity | SISA가 Transformer보다 우수 |
| Addition Mod K | SISA가 Transformer보다 우수 |
| Flip-Flop | SISA가 Transformer보다 우수 |

### 8.3 Trained-Length Retrieval (L ≤ 2048)

학습 길이 내에서의 retrieval 능력. "Long-context superiority"로 프레이밍하지 않음.

| 벤치마크 | 기대 |
|---------|------|
| NIAH (L ≤ 2048) | Mamba-2 ≪ SISA ≈ Transformer |
| Associative Recall | Mamba-2 ≪ SISA ≈ Transformer |
| RULER subset (L ≤ 2048) | Mamba-2 < SISA ≈ Transformer |

### 8.4 Selectivity / Concentration Analysis

환각과 직접 연결하지 않음.
"SSM 신호가 attention을 더 선택적으로 만드는가?" 분석.

| 측정 | 기대 |
|------|------|
| Attention Entropy | Transformer > SISA (더 선택적) |
| 관련 토큰 집중도 | Transformer < SISA (더 정확) |
| Attention Weight 시각화 | SISA가 더 선명한 패턴 |

**프레이밍**: 각 축에서 "SISA가 두 baseline 중 더 강한 쪽과 동등하거나 초과하는가?"

---

## 9. Ablation Study (Phase 1 스케일)

### 9.1 핵심: SSM 구조 기여 분리 (필수)

리뷰어 질문 1: "이거 그냥 head dim 늘린 attention 아니야?"
리뷰어 질문 2: "그냥 MLP를 줄인 효과 아니야?"

| 이름 | 내용 | 검증 |
|------|------|------|
| **(A) Transformer** | 기준선 (d_ff=3072) | 메인 baseline |
| **(A') Reduced-MLP Transformer** | d_ff=2748 (SISA와 동일) | MLP 축소 효과 분리 |
| **(C) Full SISA** | decay + rotation | 메인 |
| No decay | C̄, B̄에서 감쇠(g) 제거 | decay 기여 |
| No rotation | C̄, B̄에서 RoPE(Φ) 제거 | 복소 역학 기여 |
| **Bilinear control** | Q̂=[q,u], K̂=[k,v], decay/rotation 없는 learnable | SSM 구조 vs 단순 dim 확장 |

**Bilinear control이 가장 중요한 ablation:**
decay/rotation 없이 동일 augmented dim의 learnable bilinear term.
SISA가 이걸 이겨야 "SSM 구조가 의미 있다"를 주장 가능.

**Reduced-MLP Transformer가 두 번째로 중요:**
SISA가 (A)와 비슷하고 (A')보다 나으면 → "SSM이 줄인 MLP 용량을 보상하고도 남는다."
SISA가 (A')보다 나쁘면 → "SSM branch가 MLP 용량 손실을 메꾸지 못한다."

### 9.2 λ 변형

| 설정 | 확인 |
|------|------|
| λ = 0 (attention-only SISA control) | SSM 기여 없이 아키텍처만 다른 control |
| λ = learnable scalar (전체 공유) | 최소 설정 |
| λ = learnable per-head | 메인 설정 |
| λ = learnable per-head per-layer | 최대 유연성 |

주의: λ=0은 Transformer가 **아닌** "attention-only SISA control"이다.
MLP 크기(d_ff=2748)가 다르므로 Transformer baseline과 동일한 아키텍처가 아님.

### 9.3 d_state

| d_state | augmented_dim | SSM 추가/layer |
|---------|---------------|---------------|
| 16 | 80 | ~0.37M |
| 32 | 96 | ~0.75M (메인) |
| 64 | 128 | ~1.5M |

### 9.4 Pre-conv (선택적)

SSM 입력에 size-2 causal conv를 추가하여 인접 토큰 맥락 반영.
이는 Mamba-3의 trapezoidal 이산화에서 영감받은 입력 스무딩이다.

| 설정 | 내용 |
|------|------|
| Without pre-conv | 메인 (단순) |
| With pre-conv | x̃ = (1-β)·x + β·x_{t-1}, B=W_B(x̃), C=W_C(x̃) |

---

## 10. 일정

```
Day 0 (반나절)
  환경 세팅 + 데이터 다운로드
  Mamba-2 exact param count 확정 (sum(p.numel()) → layer 수 고정)
  SDPA flash backend smoke test 실행

Day 0 (오후)
  Phase 0 — 10M toy model sanity check (30분)
  체크리스트:
  □ NaN/Inf 없음
  □ SDPA flash backend가 augmented_dim=96에서 잡히는지 확인
  □ λ가 양수로 학습됨 (0으로 안 죽음)
  □ α effective half-life가 10~200 범위
  □ max|g - c| < 20 (FP32 exp 안전 범위)
  □ λ 분포 로깅 (wandb)
  □ Parity task에서 SISA > baseline
  □ Q̂^T K̂ / √d_h = qk/√d_h + λC̄B̄ 수치적으로 일치 확인

Day 1~2
  Phase 1 — 152M × 5B × 3 메인 모델 (A, B, C) (MIG: 각 ~45시간, 순차 실행)
  Phase 1 — 143M × 5B × 1 보조 control (A') (MIG: ~42시간)
  → SISA 작동 여부 즉시 판단

  통과 기준:
  ✅ SISA PPL ≈ Transformer ± 3%  AND  Parity > Transformer
  ✅ SISA > Reduced-MLP Transformer (A')
  ❌ SISA PPL > Transformer + 5%  → 중단
  ⚠️ λ → 0 수렴 → 초기화/LR 조정 후 재실험

  ※ MIG 1g.10gb 환경 기준. Full H100 80GB 사용 시 Phase 1 전체 ~12.6시간.
  ※ MIG 해제 가능 시 일정을 대폭 단축할 수 있음.

(이하 일정은 Full H100 80GB 기준, MIG 환경 시 약 14배 곱하기)

Day 2~3    Phase 2 — Transformer 369M × 20B (~31시간)
Day 3~4    Phase 2 — Mamba-2 369M × 20B (~31시간)
Day 4~5    Phase 2 — SISA 369M × 20B (~31시간)
Day 5~6    Phase 2 — Reduced-MLP Transformer 337M × 20B (~28시간)

Day 7      전체 평가 (벤치마크 + NIAH + RULER subset)

Day 8~9    Ablation (152M × 5B, ~3.2시간/모델)
           - 핵심 분해 4개 (no decay, no rotation, bilinear, pre-conv)  ~13시간
           - λ 변형 4개  ~13시간

Day 10     Attention 시각화 + per-head λ 분석 + 결과 정리

Day 11~15  논문 작성
```

**총: 실험 ~10일 + 논문 ~5일 = 약 2주**

**학습 중 모니터링 (wandb 로깅 필수):**
- `max|g - c|`: 20 이상이면 FP32 exp overflow 위험 → α 초기화 재조정
- `λ per-head 분포`: 어떤 head가 SSM을 많이 쓰는지
- `effective half-life`: log(0.5) / mean(log α) → 너무 짧거나(< 5) 길면(> 500) 경고
- `loss`: 3개 모델 동시 비교 대시보드

---

## 11. 논문 구성안

**제목:**
SSM-Informed Softmax Attention:
Score-Level Fusion of Selective State Tracking and Global Token Retrieval

**구성:**

1. **Introduction**
   - Transformer와 SSM의 상보적 강약점
   - 기존 하이브리드: block/head/output 수준 결합 (Jamba, Hymba, Falcon-H1)
   - SISA: score 수준에서 직접 fusion (차별점)

2. **Background**
   - Softmax Attention
   - Mamba-2 SSD duality
   - Complex SSM ↔ data-dependent RoPE (Mamba-3에서 영감)

3. **Method**
   - 3.1 SISA Score (additive formulation)
   - 3.2 Augmented Q/K Exact Rewrite (Proposition + 증명)
   - 3.3 SSM Channel Design (decay, complex RoPE, 수치 안정성)
   - 3.4 Implementation (SDPA path, scaling 규칙)

4. **Experiments**
   - 4-way comparison: Transformer, Reduced-MLP Transformer, Mamba-2, SISA
   - 152M + 369M scale
   - 4가지 평가 축

5. **Analysis**
   - Selectivity/concentration
   - Per-head λ 분석
   - Attention weight 시각화

6. **Ablation**
   - Bilinear control (핵심 방어)
   - Reduced-MLP control (MLP 축소 효과 분리)
   - Decay/rotation 분리
   - λ, d_state

7. **Related Work**
   - Hybrid architectures (block/head/output level)
   - Efficient attention (linear, sparse)
   - SSM-attention connections (SSD duality)

8. **Discussion + Conclusion**
   - 한계: O(L²) 유지
   - 향후: packing 지원 (segmented reset + parallel scan)
   - 향후: sub-quadratic SISA (linear attention + SSM bias?)
   - 향후: 스케일 확장 (1B+)

**타겟**: NeurIPS 2026 / ICML 2027

---

## 12. 리스크 및 대응

| 리스크 | 확률 | 대응 |
|--------|------|------|
| λ → 0 수렴 | 중간 | 별도 LR (10× 작게), 초기값 조정, per-head 자유도 |
| Bilinear control에 못 이김 | 중간 | decay/rotation ablation으로 각각의 기여 입증 |
| PPL 악화 | 낮음 | λ가 작으면 attention-only에 수렴 (안전장치 내장) |
| Mamba-2 파라미터 매칭 어려움 | 중간 | expand/layer 조정, **Phase 1 전에 count 확정** |
| SDPA flash backend 미작동 | 낮음 | **Phase 0 smoke test로 사전 확인**, math backend fallback |
| d_ff 축소로 MLP 능력 저하 | 낮음 | Reduced-MLP control(A')로 정확히 측정 |
| g cumsum 발산 → exp overflow | 낮음 | c offset + FP32 + **max\|g-c\| 모니터링** (< 20 유지) |
| α가 학습 중 너무 작아짐 | 낮음 | effective half-life 모니터링, 이상 시 경고 |

---

## 13. 후속 연구 (v3 로드맵)

본 실험에서 SISA의 score-level fusion이 효과적임이 확인되면:

| 단계 | 내용 |
|------|------|
| v3.1 | Document packing + segmented parallel scan + FlexAttention block_mask |
| v3.2 | MIMO (R=2, 4) augmented_dim 확장 |
| v3.3 | Pre-conv (learnable size-2 conv) 통합 |
| v3.4 | 1B+ scale 실험 |
| v3.5 | Sub-quadratic SISA (linear attention 기반으로 O(L²) 제거) |